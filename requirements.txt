fastapi
uvicorn[standard]
yfinance
anthropic
pydantic>=2.0
supabase
python-jose[cryptography]
lxml
anyio
httpx
plaid-python
dnspython

# RAG grounding for stock analysis — CPU-only torch keeps the deploy lean
# (no CUDA build; embedding runs fine on CPU for this workload)
--extra-index-url https://download.pytorch.org/whl/cpu
torch
sentence-transformers
chromadb
